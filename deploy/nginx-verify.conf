# Ownership checks from Enamad, Google and friends: a plain file that must
# answer 200 on http and https, with and without www. Verifiers usually do not
# follow redirects, so this include goes in every server block.
location = /63581236.txt { root /var/www/verify; default_type text/plain; }
location = /googleaadce0dda8a222de.html { root /var/www/verify; default_type text/html; }
